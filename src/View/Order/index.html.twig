{% extends 'layout.html.twig' %} {% block content %}
<h1>Orders</h1>
<ul>
    {% for order in orders %}
    <li><a href="/order/show/{{ order.id }}">{{ order.id }}. {{order.title }}</a></li>
    {% else %}
    <li>Nothing to display</li>
    {% endfor %}
</ul>
{% endblock %}