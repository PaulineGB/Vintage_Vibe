<section id="contactNewsletter">
	{% if errors is not empty %}
		{% for error in errors %}
			<p>{{error}}</p>


		{% endfor %}
	{% else %}
		<p>{{ sentence}}</p>
	{% endif %}


	<form method="post" classe="container">
		<div class="mb-3">
			<div class="row">
				<input type="hidden" value="{{ newsletter.id }}" name="id">
				<label class="col" for="email">NEWSLETTER</label>
			</div>
			<div class="row-2">
			<div class="mt-3">
			<div class ="align-items-center">
				<input class="col-9 justify-content-center" type="text" value="{{ newsletter.email }}" name="email" placeholder="Enter email">
				<a href="/Home/newsLetter/{{ newsletter.id }}" class="text-reset"><div class="mb-1">
<span class="col-1 justify-content-center material-icons"> send</span></a>
			</div>
			</div>
			</div>
		</div>
	</form>
</section>
