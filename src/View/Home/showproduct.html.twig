{% extends 'layout.html.twig' %}

{% block content %}
<span class="material-icons"><a herf="%">arrow_back</a></span>
<div class="text-center">
    <h1> {{product.title}} </h1>
    <h2> {{product.artist}} </h2>
    <div class="productpicture">
        <div class="row mt-3">
            <div>
                <img src="{{ product.picture }} " width="30%" height="30%" class="img-fluid">
            </div>
            <div class="mt-3">
                <p class="fs-4">About ▼▼ {{ product.description }}</p>
            </div>
            <div>
                <p class="fs-4">Category ▼▼ {{ product.category_name}}</p>
            </div>
            <div>
                <p class="fs-4">Size ▼▼ {{ product.size_name }}</p>
            </div>
            <div>
                <p class="fs-4">Price ▼▼ {{ product.price }}$</p>
            </div>
        </div>

            <button type="submit" class="btn btn-secondary mb-5" name="submit">ADD TO CART<span class="material-icons">
            shopping_cart
            </span></button>
    </div>
</div>
{% endblock %}